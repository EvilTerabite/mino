import { createTheme } from "@mui/system";

const theme = createTheme({
    palette: {
        primary: {
            main: "#bfbdc1ff"
        },
        secondary: {
            main: "#6d6a75ff"
        }
    }
})


export default theme;